# Tasks

- [ ] **Server: Add `/project` command handler** — extend the command parser in `server/routes/topics.ts` with `create`, `open`, and info subcommands
- [ ] **Client: Add `/project` to slash commands** — add entry in `client/src/components/Chat/ChatInput.tsx` commands array
- [ ] **Test: E2E tests for project commands** — Playwright tests covering AC-1 through AC-7
