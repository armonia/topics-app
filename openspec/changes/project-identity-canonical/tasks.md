# Tasks

- [x] `canonicalProjectPath()` + test (link, cartella inesistente, barra finale, `~`, vuoto)
- [x] Applicata a POST /api/topics, PATCH /api/topics/:id, `resolveProjectRef`
- [x] Test di rotta: un topic creato su un link risulta legato alla cartella vera
- [x] `scripts/canonicalizza-progetti.ts` con prova di default
- [x] `bun test server`: nessun rosso nuovo (9 pre-esistenti, licenza + git in worktree)
- [ ] Eseguire la fusione sui 4 progetti sdoppiati (richiede l'ok di Attilio + riavvio)
