name: interactive-claude-primitive
created: 2026-06-01
status: approved
supersedes: refactor-master-into-kanban
note: |
  Implemented 2026-06. Master = interactive claude PTY tab (subscription, Crown
  icon, chrome with starter prompts + /loop auto-pilot + "Aggiorna proposte");
  session read/write/close MCP tools (live-PTY tested); free attention monitor
  (user-toggled) + digest toast; Agents Board (status + preview + recommended
  action + safe-closure Autopilot) on the global board. Single 'open-master'
  path (sidebar/⇧⌘M/board buttons); MasterBoardStrip + chat-master removed.
  Remaining (optional / user-side): runtime E2E of the full loop; persist the
  master role server-side (currently name-based detection).
