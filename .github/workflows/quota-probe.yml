name: Sonda quota artifact
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "sonda $(date -u)" > probe.txt
      - uses: actions/upload-artifact@v4
        with:
          name: quota-probe
          path: probe.txt
          retention-days: 1
