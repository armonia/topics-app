// Gate 1: does cef-rs compile + download the CEF binary distribution on this
// macOS/Rust setup? Empty main — the point is to build the `cef` crate.
fn main() {
    println!("cef-spike: crate linked");
}
