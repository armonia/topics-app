# Topics App — Audit di consolidamento

_2026-07-06 · follow-up operativo dell'audit multi-agente [2026-06-19](AUDIT-2026-06-19.md) · fix applicati con agenti paralleli, verifica per-finding, commit tematici revertibili._

## Obiettivo

Rendere l'app «solida, pulita, refactorata e standard, esente da bug» tracciando ogni superficie. A differenza del 2026-06-19 (che era **diagnostico**: 385 finding, voti per dimensione), questo giro è **operativo**: prende i finding ancora vivi, li ri-verifica sul codice, e li chiude in commit isolati — oppure li rimanda con motivazione esplicita.

Tre vincoli decisi con l'umano prima di agire:

1. **Security LAN** — la produzione si raggiunge via **Tailscale**, non da rete ostile. Quindi NIENTE hardening di auth WS/filesystem in questa sessione (romperebbe mobile/PWA sulla LAN fidata); solo quick-win innocui. A1/A2 documentati sotto per una sessione dedicata futura.
2. **Dead code** — cancellare solo dopo aver verificato migrazione completa / zero consumer vivi, una tranche per commit, revertibile.
3. **Test/CI** — cablare un vero job E2E in CI (con build client), ripulire le spec, isolare gli env.

## Commit di questa sessione

| Commit | Area | Cosa |
|---|---|---|
| `30484d10` | security | gateway-token check timing-safe (`timingSafeEqualStr`); Vite `allowedHosts` ristretto da `true` a `localhost`/`127.0.0.1`/`.ts.net` |
| `dee872fc` | correctness | guard try/catch su `autoBindProject` (crash su setTimeout non catturato); rimosso overlay reload duplicato nel terminale |
| `3e7cab90` | chat | rilascio microfono su unmount (`useVoiceCall`); errori di upload ora emergono via toast; `OverflowMenu`→`<Menu>` |
| `9867fc5f` | git/editor | conferma azioni distruttive (branch force-delete a doppia conferma, file delete via dialog); guard race su diff-fetch; arg corretto a `ScriptRunner.stop` |
| `ad2ac03a` | modals/menus | guard su save falliti (`updateTopic` null → toast+rollback); `role=dialog`; geometria menu misurata reale invece che stimata |
| `b17f609e` | browser | coda download scoped per-pane (Rust `pane_id`); handle nativo memoizzato; debug DnD gated dietro `import.meta.env.DEV` |
| `4cb6a777` | layout | `dropEffect='move'` sul dragover del container `PaneTabBar` (WKWebView pop-out-close guard) |
| `76cd6cf1` | dead-code | rimosso `agent-epoch-store.ts` + test (zero consumer vivi; migration SQL 025 resta) |
| `96a0d5a6` | test/CI | job E2E reale in `ci.yml`; helper estratti (`tests/e2e/helpers/layout.ts`); isolamento `OPENCLAW_DIR`/`HOME` |
| `ae7fbc1b` | dead-code | rimossi namespace morti `lib/api.ts` (unread/processes/usage/topicMessages/machines) + tipi esclusivi + stub mock |
| `201c9a6e` | layout (W2) | registra il pane su `topic:switch`; `PURGE_ORPHAN_PANE` su close project/terminale; estratto `notifyPaneReflow()` |
| `ad7e1c3f` | state (W2) | dedup backoff (`syncBackoff.ts`); registrato `ui-state:patch`; campi legacy morti rimossi; +3 test (422 pass) |
| `d44d095a` | tauri (W2) | comandi self-test IPC gated a `debug_assertions` (release-safe); rimossa dep `window-state` + capability `shell:allow-execute` |
| `c0eb409e` | dead-code (D1) | escisso il path Electron-CDP runtime-dead (~1.2k LOC): `browser-cdp-dispatcher.ts`/`browser-cdp-raw.ts`/`electron-cdp-probe.ts` + `cdpOps()`; `resolveOps()` ora è sempre il ramo Playwright; teardown nativo `DELETE …/cdp-target` preservato |
| `13ab8b19` | dead-code (D3) | rimosso il blocco tipi Kanban morto da `lib/api.ts` (`Task`/`BoardTask`/`TaskStatus`/`Tag`/`TaskComment`/`BoardSettings`/`Approval`) + migrato `demo/api.mock.ts` off (rimossi `tasksApi`/`boardsApi`/`approvalsApi`/`tagsApi` + `MOCK_TASKS`) |
| `86c4d3ab` | test/CI (E) | rimossa la spec E2E morta `panels.spec.ts::"kanban board renders"` (nessun pane/nav Board dopo v2.0.0 → falliva la CI E1 appena cablata) |
| `ee9f34c8` | bug (tauri) | de-registra l'observer live-resize alla chiusura di una detach window: la registrazione NSNotificationCenter (`object: ns_window`) restava appesa a un NSWindow liberato (dangling / riuso indirizzo → firing spurio cross-window). `removeObserver:` nel Destroyed handler già esistente |
| `44cfcdb0` | bug (diff) | regex Aider search/replace richiedeva estensione (`.+?\.\w+`) → i blocchi per file senza estensione (Dockerfile, Makefile, `.gitignore`, LICENSE, `.env`) cadevano a testo. L'anchor vero è `<<<<<<< SEARCH`; +`diffParser.test.ts` |
| `6f16d30f` | bug (chat) | soft-grace/hard-timeout finalizzavano al client ma NON abortivano il provider → il turn spawnato continuava, rifirava `onDone`→`finalizeStream` una seconda volta (ri-persist sopra il msg 'timed out', ri-broadcast) e la promise-queue per-sessione restava bloccata (topic freezato fino al timeout 30-min del provider). Ora entrambi abortiscono il provider + `finalizeStream` idempotente |
| `f1cec99a` | bug (chat) | `planMode` seedato una volta da localStorage in `useState`, ma `ChatPane` non è keyed su `topic.id` → switch in-place (`StandaloneChatGroup`) trascinava il valore del topic precedente (Plan Mode di A perdeva su B). Effetto reconcile su `topic.id`, come i reconcile `fastMode`/scroll già esistenti |
| `74dfeaba` | bug (server) | due blocchi `if (event.event === "agent")` fratelli: il ramo thought/thinking era DOPO il guard `stream !== "tool"` → codice morto irraggiungibile. Fuso in un solo blocco col thinking-handler PRIMA del guard → i delta di reasoning dell'agent arrivano a `onThinkingDelta` |
| `1468339e` | bug (server) | `isStreaming()` su stale faceva `activeStreams.delete()` nudo → orfanava per sempre i messaggi `partial=1`. Rimosso il delete: finalize demandato allo sweeper autoritativo (`staleStreamTimer`, 3-min) che chiude la riga DB + broadcasta `stream:end` |
| `01e7e920` | correctness (server) | 3 type-guard su body non validati (system-message `content`, POST `/api/topics` `name`, PATCH `name`) → prima 500/scrittura parziale su input non-stringa. NON è l'hardening auth deferito (A1/A2): è validazione di tipo |
| `d29d1d37` | bug (client) | `ProcessLogPane` poll 2s si sovrapponevano (offset avanza solo dopo await) → testo log duplicato: `inFlight` guard. `useActivity` buffer in pausa illimitato → cap a `MAX_CLIENT_EVENTS` (memory leak) |
| `7e31718d` | bug (server) | `sendPushToAll` (getDatabase/initVapid al top non catchati) chiamata fire-and-forget in 4 punti → unhandled rejection. Wrapper `firePush()` con `.catch` |
| `d7f7eacf` | bug (client) | Effect 7 di validazione spingeva i project-pane in `openPanels` senza `ensurePaneRegistered` → l'orphan-guard di `REORDER_PANES` li droppava (tab mai aperto). Registra l'entità `{type:'project', projectPath}` prima. + dedup close-panel doppio-click (undo spurio) |
| `eadfa17b` | bug (client) | mic caldo dopo End Call (i branch restart di `onstop` catturavano `isCallActive` stale → ref-mirror `isCallActiveRef`); listener `visualViewport resize` anonimo mai rimosso in `useMobile` (leak per-mount); guard close-panel |
| `34269358` | bug (tauri) | `browser_open` sul ramo label-esistente rifaceva `browser_navigate` a ogni remount grace-window → ricaricava la WKWebView viva. Ora confronta `wv.url()` col target e salta il navigate se già lì |
| `5f31072f` | bug (client) | `mergeCatchupIntoPartial` nel ramo create-new cadeva su `lastMessage.toolCalls` → una bubble nuova ereditava la tool-card del turn precedente finalizzato. Fallback scoped al solo ramo partial-merge; +test |

## A — Security

### Applicato (quick-win innocui)
- **Timing-safe token compare** (`30484d10`): i confronti di `GATEWAY_TOKEN` in `routes/terminal.ts` e `routes/topics.ts` passavano per `===` (timing-leak teorico); ora via `timingSafeEqualStr()` (`server/utils.ts`, `crypto.timingSafeEqual` con length-guard).
- **Vite DNS-rebinding** (`30484d10`): `allowedHosts: true` accettava qualsiasi Host header; ristretto a localhost + MagicDNS Tailscale (`.ts.net`). Accesso via IP-literal Tailscale bypassa comunque il check lato Vite (documentato inline).

### Rimandato — sessione dedicata (decisione Tailscale)
- **A1 — server bind `0.0.0.0` senza default-deny auth.** Il server espone tutta la superficie `/api` file/git/memory/upload/browser-control + 3 canali WS a qualunque peer LAN/Tailscale. **Non toccato di proposito**: serve per mobile/PWA sulla LAN, e la rete è ristretta a Tailscale. Un vero fix è una dispatch-boundary default-deny (auth opt-out per-route, non opt-in), non un patch. → sessione security dedicata.
- **A2 — path-containment.** `resolveSafePath()` (l'unico resolver confinato, `utils.ts:1029`) ha zero caller; `resolveProjectPath()` (non confinato, `utils.ts:1051`) è usato ~53 volte. Traversal/SSRF sono primitive vive. Fix = far passare le rotte file/git per il resolver confinato — refactor ampio, va fatto con test di regressione dedicati. Vedi `SECURITY.md` (già dichiara local-first/no-auth/esposizione a rete non fidata fuori scope).

## B — Correttezza (bug reali chiusi)

- `autoBindProject` non era in try/catch: un throw dentro il `setTimeout` sarebbe risalito come unhandled → `chat.ts` (`dee872fc`).
- Overlay «reloading» duplicato nel terminale (doppio blocco `{reloading && !stale}`) → `SingleTerminalPane.tsx` (`dee872fc`).
- Microfono/TTS mai rilasciati se il componente voice-call smontava mid-call → cleanup su unmount in `useSpeech.ts` (`3e7cab90`).
- Fallimenti upload (inclusi `!res.ok`) silenziosi → ora raccolti e mostrati via toast in `ChatPane.tsx` (`3e7cab90`).
- Branch force-delete senza conferma esplicita; `ScriptRunner` passava `processId` dove serviva `scriptName` (stop del processo sbagliato); race su diff-fetch al cambio file; file-delete via `window.confirm` → dialog `MODAL_PANEL` (`9867fc5f`).
- `updateTopic` ritorna `Topic|null` ma il salvataggio non controllava il null (perdita silenziosa di modifiche); unlink progetto senza rollback su fallimento → `TopicSettingsModal.tsx` (`ad2ac03a`).
- Coda download browser globale (mescolava eventi tra pane) → scoped per-pane fino al layer Rust (`b17f609e`).
- `PaneTabBar` container dragover non settava `dropEffect` → su WKWebView il tab «scompariva» in pop-out-close (`4cb6a777`).

## C — Design system (coerenza)

- Menu ad-hoc → primitive `<Menu>`: `OverflowMenu` (`3e7cab90`), `CommandMenu` (`ad2ac03a`).
- Geometria menu **stimata** (dimensioni hardcoded) → **misurata** reale via `computeMenuPosition`/`useLayoutEffect`: `ContextMenu`, `PaneAddMenu` (`ad2ac03a`).
- Tint di selezione blu/verde ad-hoc → `SELECTED_SURFACE` unico: `BranchList`, `GitChanges`, `FileExplorer`, `FileSearch` (`9867fc5f`).
- `role="dialog"`/`aria-modal` mancanti su `KeyboardShortcuts` (`ad2ac03a`).

## D — Dead code (verificato migrato prima di cancellare)

- **D2 fatto** (`76cd6cf1`): `server/agent-epoch-store.ts` (380 LOC) + suo test — unico consumer era il test stesso. La migration numerata `025-agent-epochs.sql` resta (storia append-only; le tabelle orfane sono innocue).
- **D1 fatto** (`c0eb409e`): il path Electron-CDP (`browser-cdp-dispatcher.ts`, `browser-cdp-raw.ts`, `electron-cdp-probe.ts`, ~1.2k LOC coi call-site) era **runtime-dead** (probe su :19333 sempre falso post-Electron + il native delegate Tauri intercetta a monte) ma **compile-coupled** in 5 file server + 2 test. Provato dead prima di cancellare (vincolo «sicuri sia tutto migrato»): `resolveOps()` ora ritorna sempre `playwrightOps`; rimosso `cdpOps()`; il teardown nativo su `DELETE /api/browsers/:id/cdp-target` (`clearBrowserCaches`+`resetMoondreamCounter`) è **preservato**. 64 test browser verdi (incluso il ramo `browser_import_chrome [native]`).
- **D3 fatto** (`ae7fbc1b` + `13ab8b19`): namespace morti in `lib/api.ts` con **zero call-site reali** rimossi nella prima tranche (`unreadApi`/`processesApi`/`usageApi`/`topicMessagesApi`/`machinesApi` + tipi esclusivi). Il blocco tipi Kanban (`Task`/`BoardTask`/`TaskStatus`/`Tag`/`TaskComment`/`BoardSettings`/`Approval`) era l'ultimo residuo — morto nell'app ma ancora referenziato dal mock demo; migrato `demo/api.mock.ts` off (rimossi import riga 6 + `TASK`/`MOCK_TASKS` + `tasksApi`/`boardsApi`/`approvalsApi`/`tagsApi`) **prima** di cancellare i tipi. `UnreadData` NON toccato (tipo WS condiviso vivo in `types/index.ts`).

## E — Test & CI

- **E1 fatto**: job `e2e` reale in `.github/workflows/ci.yml` — gira l'intera suite Playwright contro il server isolato (:13334, DB SQLite proprio), non più solo `playwright test --list` (che parsava le spec senza eseguirle). Build client (il server serve `public/`), rebuild `node-pty` per Linux, `CHROMIUM_PATH` risolto dal binario installato, report artifact su failure.
- **E4 fatto**: `layout-fixes.spec.ts` cancellata (seed su chiavi legacy, l'app legge `pane-store-v2` — stale).
- **E2 fatto**: helper duplicati estratti in `tests/e2e/helpers/layout.ts`; 6 spec migrate.
- **E5 fatto**: isolamento env (`OPENCLAW_DIR`/`SESSIONS_DIR`/`HOME`) in `global-setup.ts` + `start-test-server.sh`.
- **E3 fatto** (`86c4d3ab`): spec-cleanliness residua — rimossa la spec E2E morta `panels.spec.ts::"kanban board renders"`. Puntava a un bottone `/Board/` che non esiste più (i pane utility sono `activity`/`agents`/`dashboard`/`journal`/`cron`; nessun tipo `board`), quindi aspettava 10s un elemento mai montato → **fallimento garantito** ora che E1 esegue davvero la suite. Feature verificata rimossa (nessun pane/nav Board, tipi Kanban tolti in D3) prima di cancellare il test.
- **E6 rimandato — espansione di copertura, sessione dedicata.** Non è pulizia: è coverage netta nuova. Il core value dichiarato (E2E completa) ha ancora buchi reali: (a) il motore di streaming chat reale è solo mock-SSE/gateway-skip — zero copertura un-mocked (`chat.spec.ts`); (b) i confini di sicurezza server (path-traversal/SSRF/eval) non hanno negative-test d'integrazione; (c) ~42 test dashboard/agent sono mock-fed contro backend dormienti (bassa fedeltà, ma i pane **bootano** ancora → non sono dead-feature, quindi restano); (d) placeholder `test.fixme` permanenti (`notifications-non-invasive.spec.ts`). Rimandato perché aggiungere copertura reale a streaming/sicurezza è un lavoro ampio e a sé, non un bug da chiudere qui.

## Wave 2 — follow-up verificati (layout / state / tauri)

Tre agenti paralleli su aree disgiunte; ogni diff **ri-verificato a mano** sul codice prima del commit (i finding critici avevano precedenti falsi positivi in questo repo).

**Layout** (`201c9a6e`)
- **P0.1 — `topic:switch` non registrava il pane.** L'handler WS spingeva `toTopicId` in `openPanels` senza `ensurePaneRegistered` prima → l'orphan-id guard di `REORDER_PANES` (Effect B) lo droppava e il tab non compariva. Fix con la shape **identica** a quella canonica di `openPanel()` (`{id: topicId, type:'chat', topicId, title}`, `group:default`) — verificato leggendo `openPanel` riga 1001. Impatto stretto: morde solo quando passa la guardia `isOwnStream`.
- **P1.2 — leak pane su close.** `handleCloseProject`/`handleCloseTerminal` filtravano solo `openPanels`, lasciando l'entità in `state.panes` per sempre; peggio, un filtro nudo lascia che `REORDER_PANES` re-appenda il tab chiuso. Fix: `PURGE_ORPHAN_PANE` (nessun tombstone: project/terminale non hanno undo). Verificato che l'action esista (`types.ts:285`, test esaustivi) e che `paneId = createPaneId('project'|'terminal', …)` combaci con la registrazione.
- **P1.3 — dedup.** Estratto il segnale sintetico di reflow browser (dispatch `pane-resize-start` + 400ms `pane-resize-end`) in `notifyPaneReflow()`; entrambi i call-site migrati.

**State** (`ad7e1c3f`)
- Dedup del `backoff()` byte-identico di `tombstoneSync` + `projectLayoutSync` in `syncBackoff.ts` (il `putWithRetry` con AbortController-coalescing di `syncServer.ts` lasciato come consolidamento futuro).
- Registrato `ui-state:patch` nello schema `ws-inbound` (loose): frame letto a mano da 3 moduli, assente dal registro che l'header dice deve coprirli tutti.
- `importLegacy` idratava `closedTabs`/`gridLayout`/`projectLayout` mai letti da `HYDRATE_FROM_LEGACY` e poi cancellati → rimossi da payload + tipo.
- +3 test (durability tombstone, `paneConfig`, caso `SPLIT` del reducer). **422 pass / 0 fail.**

**Tauri** (`d44d095a`)
- Comandi self-test (`fps_report`/`focus_report`/`focus_grab_browser`/`focus_grab_window`) erano in `generate_handler!` **incondizionati** → invocabili via IPC da qualunque JS in release (scrivono stringhe del chiamante su path `/tmp` fissi; rubano il first-responder AppKit). Gated **sia la fn sia l'entry della lista** dietro `#[cfg(debug_assertions)]`. **`cargo check` verde in debug E release** (la simmetria fn+entry evita il riferimento penzolante in release).
- Rimossa dep `tauri-plugin-window-state` (mai `.plugin()`-registrata; solo commenti la citano); rimossa capability inutilizzata `shell:allow-execute` (tenuto `shell:allow-spawn`); tolto un blocco `unsafe` interno ridondante.

## F — Bug-hunt fan-out (4 agenti, triage a mano)

Quattro agenti read-only su superfici disgiunte (server / layout / shared / tauri). Ogni finding **verificato leggendo il codice reale** — questo repo ha storia di falsi positivi "critical", quindi soglia alta: si fixa solo ciò che ha un failure-path concreto.

**Confermati e chiusi (2):**
- **Tauri — observer live-resize appeso** (`ee9f34c8`). `window_detach` costruisce una vera seconda NSWindow top-level e chiama `wire_live_resize_cover`, che registra un observer NSNotificationCenter filtrato `object: ns_window`. La registrazione non veniva mai tolta → alla chiusura della detach window il centro notifiche teneva un puntatore a NSWindow liberato (l'indirizzo può essere riusato da una finestra successiva → firing spurio cross-window). Il commento del file **predice esattamente** questo scenario ("if a SECOND top-level NSWindow is ever introduced AND can close…"). Fix: mappa `live_resize_observers()` keyed sul puntatore NSWindow + `removeObserver:` nel Destroyed handler già presente (stesso ciclo di vita delle mappe vibrancy che lì si purgano per lo stesso motivo di riuso-indirizzo).
- **Shared — diff Aider senza estensione** (`44cfcdb0`). La regex richiedeva `.+?\.\w+` sul path → i blocchi per Dockerfile / Makefile / `.gitignore` / LICENSE / `.env` cadevano a testo semplice. L'anchor forte è `<<<<<<< SEARCH`; il path è solo la riga non-vuota precedente. +`diffParser.test.ts` (4 test).

**Respinti come falsi positivi / by-design (verificati sul codice):**
- `filesApi.search` boolean→string: il client manda `String(regex)`→"true"/"false", il server legge `=== "true"` (`files.ts:195-196`). Contratto corretto, non un bug.
- `browser-service.ts` `destroyContext` non-awaited nel reaper: fire-and-forget **voluto** (context indipendenti, `.catch` guarda i reject); la mutazione async di `contexts` è documentata dall'autore e il reap Chromium è gated su `contexts.size === 0` (settle al tick dopo).
- `syncServer.ts:108` AbortError: è il **coalesce path** (un PUT più nuovo ha già preso il posto), il return silenzioso è LWW by-design.
- `projectLayoutSync` passthrough di entry null: hardening speculativo, nessun path mostra che `null` entri in `nonChatPanes`.
- Resto (mutation-during-iteration su JS single-thread, overflow u32→i32 impossibile, REORDER filtering by-design, ordine chiavi deterministico, fragilità latenti 1:1): non-bug.

## G — Bug-hunt fan-out, seconda tornata (triage a mano)

Secondo giro di agenti read-only su superfici disgiunte (server / stream-lifecycle / hooks client / tauri / chat), stessa soglia alta della sezione F: si fixa solo con failure-path concreto, ogni finding ri-letto sul codice. **11 bug confermati e chiusi** in commit isolati (2 chat + 4 server + 4 client + 1 tauri), tabella sopra.

**Filo conduttore dei confermati:**
- **Stream-lifecycle server** — tre bug indipendenti sullo stesso asse (finalize del turn): il timeout non abortiva il provider (`6f16d30f`, doppio-finalize + queue bloccata), `isStreaming()` orfanava le righe partial invece di lasciarle allo sweeper autoritativo (`1468339e`), e il ramo thinking dell'agent era codice morto dietro un guard (`74dfeaba`).
- **Idempotenza / mirror di stato client** — Plan Mode che perdeva tra topic per un `useState` non-keyed (`f1cec99a`), mic caldo per una closure stale su `isCallActive` (ref-mirror, `eadfa17b`), catchup che ereditava la tool-card del turn precedente (`5f31072f`): tutti bug da "valore letto nel momento sbagliato".
- **Contratto register-before-open** — Effect 7 ripeteva l'esatto errore già chiuso su `topic:switch` in Wave 2 (`201c9a6e`): id in `openPanels` senza `ensurePaneRegistered` → orphan-guard di `REORDER_PANES` lo droppa (`d7f7eacf`).
- **Leak / duplicazione senza guard di rientro** — poll log sovrapposti (`d29d1d37`), buffer attività illimitato (`d29d1d37`), listener viewport mai rimosso (`eadfa17b`), push fire-and-forget non catchato (`7e31718d`).

**Respinti come falsi positivi / by-design (verificati sul codice):**
- `agents.ts:205` spawn: fallirebbe solo se il gateway rispondesse 2xx + body non-JSON — speculativo, nessun path reale.
- `browser-native-state.ts` throw: input-validation **voluta** per contratto del dispatcher; entrambi i caller convertono in `{error}`.
- Race UI auto-correttive documentate ma **non fixate** (impatto reale nullo in single-user, il prossimo evento riconcilia): `tags.ts` UNIQUE TOCTOU, `FileExplorer` project-switch, `CommandPalette` search, `BranchList` checkout concorrente, `SessionHistory` agentId, `useJournal` date-nav.

## H — Tornata 3: superfici mai auditate (in corso)

Inventario completo per aree (server 37k LOC, components 42k, hooks 11.7k, e2e 25.8k…) → 6 agenti sonnet su ciò che le tornate 1-2 non avevano MAI toccato: route server residue (28 file), moduli server core + sottodirectory, componenti client (Sidebar/Shared/Modals/… e Agents/Browser/Editor/…), lib/contexts/scripts, Layout+hooks residui. Più un pass diretto su `server.ts` (1100 righe, mai in scope agente).

**Chiusi finora (13 commit):**
- `13907e51` — **native-executor unregister owner-scoped** (pass diretto su server.ts): il reaper/close di un socket stale uccideva la registrazione fresca post-riconnessione → agent ops deviate sul Playwright fantasma. Stesso pattern owner-guard poi riusato per codex.
- `3ff93bd3` — **screencast start differito 250ms** (pass diretto): su Tauri ogni pane browser lanciava un Chromium headless fantasma (register_native_executor arrivava prima del getOrCreate multi-secondo → stopScreencast no-op → stream JPEG eterno + context che bloccava il reaper Chromium).
- `d3b8fcbd` — **ai-review-screenshots.sh** morto sul proprio shebang zsh (bashismi `${!PAIRS[@]}`/`read -ra` → bad substitution alla prima coppia); caller fallback allineato (zsh via execFileSync).
- `852d049c` — **heartbeat agent-profiles type-guard**: bind non-primitivo → riga sessione scritta a metà + 500 (endpoint di reporting per agenti esterni).
- `a8298ba0` — **SIGKILL ritardato identity-guarded** (processes.ts): PID riusato entro i 5s di grazia poteva uccidere un processo innocente; bookkeeping in-process per gli script, confronto lstart per l'albero detected.
- `0a180868` — **6 bug componenti** (sweep A): ContextMenu senza key (rename/delete sul topic SBAGLIATO), useContextInspector senza staleness guard (salvava la memoria di B col testo stale di A — perdita dati), TopicSettingsModal reset silenzioso su topic:updated (auto-namer wipeava il form), CommandPalette race file-list su ⌘1-9, Virtuoso senza computeItemKey (expanded su eventi sbagliati), spinner checkout branch remoti mai mostrato.
- `4a27746c`…`7e2796ef` — **7 commit dal batch server-core** (10 finding): consent-model cookie Chrome (sanitizer svuotato → dump intero profilo), codex owner-scoped cleanup (turni sovrapposti → stop rotto), worktree delete (prune mancante + unwatchGitDir mai chiamato + GC coda inerte per identità Promise), snapshot ring mai svuotato su delete topic, scanCache transcript-title illimitate (LRU 200), timer 30-min mai cancellato + detection stderr sul chunk anziché sul buffer, corruzione usage silenziosa.

**Respinti/documented-only (tornata 3):** edit.ts role forzato sui sibling (irraggiungibile da UI); agents spawn 2xx+non-JSON (speculativo); PPID==1 reap-loop start-prod (nessun trigger riproducibile); dashboard/journal fetch-race auto-correttive. **Dormienti by-design (non cancellati):** `claude-events-watcher.ts`/`claude-tasks-sync.ts` mai importati da server.ts ma parte dell'infra tasks/approvals che tiene bootabili Dashboard/Agents — eventuale rimozione richiede il check migrazione completo (vincolo D).

**Batch c4 — Layout/hooks residui (`59ea8764`, 5 fix):** openSessionViewerPane registrava il pane come `{type:'chat'}` via fallback (tab "New Chat" con icona chat — violazione register-before-open); useMemory senza staleness guard (salvava la memoria di B col testo stale di A — stesso pattern/fix di useContextInspector); useJournal race prev/next-day; useClaudeSessionState bootstrap wholesale che regrediva rev più fresche su reconnect (merge rev-guarded, healing preservato); useTerminalLifecycle replace wholesale che droppava la sessione ottimistica in grace. **Layout/ 40 file dichiarati PULITI** su drag&drop/pop-out/geometria/mobile (soglia alta, un falso positivo scartato dall'agente stesso).

**Batch c2 — Chat/Project (`181a2bab`, 9 fix):** pill "escludi dal contesto" era UI morta (il file veniva iniettato comunque; ora toggla `disabledContextSources`, il canale che l'assembler rispetta, e ✕ rimuove davvero); vocale consegnato al topic attivo allo stop invece che a quello di registrazione; @mention con lista file del PRIMO progetto pinnata per sempre; ScriptRunner spinner rotti (scalari condivisi + confronto processId/scriptName mai vero per i detected); FileSearch race query lenta; fastMode ereditato da topic senza valore esplicito (classe planMode); useCheckpoints troncava la lista di B all'indice di A; ProcessLogPane durata ripartiva da 0 all'apertura. **Agents/Browser/Editor/Terminal dichiarate PULITE** (3 falsi allarmi scartati con verifica alla mano).

Tornata 3 **completata**: tutte le superfici mai auditate sono state passate (route server, moduli core, ~150 componenti, lib, scripts, hooks, Layout), 27 bug confermati chiusi + il redesign Drop UX v2.

## I — Drop UX v2 (richiesta prioritaria utente: "overlay brutti e poco usabili, non fa mettere in tutte le posizioni")

Root cause di usabilità trovate leggendo il codice: (1) zone di split = **bande assolute di 30px** sul bordo (`EDGE_DROP_PX`) — su un pane da 1200px "split a destra" era uno sliver immirabile, percepito come "posizione impossibile"; (2) **zero feedback sul center** (merge-as-tab sul corpo pane non dipingeva nulla e il drop era `return` secco — su WKWebView pure hazard pop-out); (3) gutter full-row da 26px con label uppercase e fill idle su OGNI drag (il "brutto"); (4) tab drag: **split verso l'alto impossibile** ('top' deviato alla bar); (5) grid drag top/bottom: preview mezza-cella ma azione riga full-width (bugia visiva).

**Shippato in 3 commit (`5f50e75a`, `27dc9b76`, `9ce88936`):**
- `detectDropZone` v2: zone RELATIVE stile VS Code (box centrale = center, anello esterno 25%/asse floored 30px = nearest-edge) — target ~10× più grandi, condiviso da entrambe le superfici. +7 unit test (incluso il punto-contratto E2E).
- Gutter senza label: idle = hairline sul bordo, fill solo su hover; nuovo primitivo `CenterRegion` (fill inset arrotondato) per il merge.
- Standalone: tab-top = **stack sopra la colonna** (nuovo: il pane droppato diventa primary, il vecchio primary demota nello stack); tab-center sul corpo = merge nel gruppo target (era drop morto); grid top/bottom = riga full-width PREVIEW onesta con la banda strip/gap accesa dal dragover della cella; strips accettano anche GRID_ITEM.
- Progetto: center sul corpo = merge nel gruppo (era drop morto); i 4 edge già completi (`addGroupToColumnStack` top+bottom) ereditano le zone v2.
- Verifica: tsc 0, 139 unit Layout + 7 dropZone verdi, build+demo ok, **verifica visiva su :3333 via dragover sintetici** (5 zone → overlay giusto, screenshot ok), **grid-split+drop-zones-v2 E2E 28/28 al primo tentativo**.

**Fix collaterali trovati dal giro E2E:** menu contestuale delle tab senza `role="menu"` (call-site sfuggito all'unificazione dropdown — ora conforme); 4 selettori `.fixed.z-[9999]` stale in grid-split.spec + GRID-09/10 che asserivano il VECCHIO comportamento di reset (pre-`abfa87f9`) o conteggi page-wide non ermetici sotto union-hydrate (il "tab perso" era il purge BY-DESIGN di un topic project-bound che squattava da tab standalone); `data-testid="project-window"` per lo scoping E2E.

## Verifica finale

**Tornata 3 (aggiornata):** `client tsc -b` = 0 · `typecheck:server` = 0 (baseline 0) · `bun test client/src/{state,components/Layout,lib}` = **713 pass** · `bun test server/` = **569 pass** · E2E `grid-split + drop-zones-v2 + tab-system-reliability` = **28 pass al primo tentativo** · build client (deployata in `public/`, hot-delivery al Topics.app live) + demo build ok · verifica visiva drop-zones sul vivo (5 zone → overlay corretto).

**Tornate precedenti:** `cargo check` (desktop-tauri) verde (warning pre-esistenti) · `bun test client/src/hooks/streamCatchupMerge.test.ts` = 14 pass · `diffParser` = 4 pass. Tutti i commit sono su `main`, non pushati.

**Chiuso in questo follow-up rispetto alla prima stesura**: D1 (Electron-CDP escisso, ~1.2k LOC), D3 (ultimo residuo tipi Kanban + mock demo), E3 (spec Board morta), F (2 bug nativi/render su 4 report bug-hunt), **G (11 bug confermati nella seconda tornata: stream-lifecycle, idempotenza stato, register-before-open, leak)**. **Restano deferiti per decisione/ampiezza**: A1/A2 (auth boundary + path-containment → sessione security, decisione Tailscale) ed E6 (espansione copertura streaming/sicurezza un-mocked → sessione test dedicata).

## Voti aggiornati (indicativo)

Rispetto al 2026-06-19, questa sessione muove **clean** e **standard** (dead code chiuso/scoped, design-system unificato su più call-site, CI E2E reale) e chiude una manciata di bug di **correttezza**. **solid** resta gated su A1/A2 (decisione Tailscale) e sul refactor D1/resolveOps. Un voto formale per-dimensione richiede un nuovo giro diagnostico completo, fuori scope qui.
